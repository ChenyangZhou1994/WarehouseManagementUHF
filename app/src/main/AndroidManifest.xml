<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.xinyun.warehousemanagementuhf">

    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/xyzy"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/xyzy"
        android:supportsRtl="true"
        android:theme="@style/Theme.WarehouseManagementUHF">
        <activity android:name=".GKPDActivity"></activity>
        <activity android:name=".FCPDActivity" />
        <activity android:name=".XDPDActivity" />
        <activity android:name=".CKPDActivity" />
        <activity android:name=".ScanHistoryActivity" />
        <activity android:name=".WriteTagActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>